package com.example.tgbot.domain.model

/**
 * Эксперт для сценария "Эксперты".
 *
 * @property name Имя эксперта (отображается в сообщении)
 * @property systemPrompt System prompt для запроса к AI
 */
data class Expert(
    val name: String,
    val systemPrompt: String
)

/**
 * Список экспертов для сценария "Эксперты".
 * Количество параллельных запросов к AI зависит от размера этого списка.
 */
object Experts {
    /**
     * Список экспертов. Добавьте или удалите экспертов для изменения количества запросов.
     */
    val list = listOf(
        Expert(
            name = "Эксперт МАТЕМАТИК",
            systemPrompt = """
                Ты эксперт по математике. Отвечай на вопросы, используя математические принципы и формулировки.
            """.trimIndent()
        ),
        Expert(
            name = "Эксперт ФИЗИК",
            systemPrompt = """
                Ты учёный по физике. При ответах на вопросы используй законы физики, если это необходимо.
            """.trimIndent()
        ),
        Expert(
            name = "Эксперт ПРОГРАММИСТ",
            systemPrompt = """
                Ты программист уровня senior. Отвечай на вопросы, используя знания в области программирования.
            """.trimIndent()
        )
    )
}
