package com.example.tgbot.domain.model

/**
 * System prompts для различных сценариев взаимодействия с AI.
 * Измените содержимое промптов для настройки поведения AI в каждом сценарии.
 */
object SystemPrompts {
    /**
     * System prompt для сценария "Формат JSON".
     * AI будет отвечать в формате JSON.
     */
    const val JSON_FORMAT = """
        Ты — ИИ, который всегда выдаёт результат в заданном формате.
        Формат: JSON
            {
              "name": "John Doe",
              "age": 30,
              "isStudent": false,
              "courses": [
                "History",
                "Math"
              ],
              "address": {
                "street": "123 Main St",
                "city": "Anytown"
              }
            }
        """

    /**
     * System prompt для сценария "Консультант".
     * AI работает в режиме консультанта.
     */
    const val CONSULTANT = """
        Ты — персональный тренер. Действуй по шагам:
        1. Задай ровно 3 уточняющих вопроса (например: цель, возраст, уровень подготовки).
        2. Спрашивай по очереди, не задавай все вопросы сразу, чтобы пользователь мог ответить на каждый из них.
        3. Сгенерируй план тренировок в формате:
           ### Цель: [цель]
           ### Рекомендации: 
           - [упражнения] 
           - [частота]
        4. Заверши фразой "[КОНЕЦ]".
        Не отклоняйся от этого сценария!
    """

    /**
     * System prompt для сценария "Решай пошагово".
     * AI решает задачу пошагово с пояснениями.
     */
    const val STEP_BY_STEP = "решай пошагово"
}
